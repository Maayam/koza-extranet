{% extends 'base.html.twig' %}

{% block body %}
<section class="popup">
    <h1>Nouvelle mission</h1>
    {% if errors is defined %}
        {% for error in errors %}
        {{ error }}
        {% endfor %}
    {% endif %}
    {{ form_start(form) }}
    {{ form_end(form) }}
</section>
{% endblock %}
